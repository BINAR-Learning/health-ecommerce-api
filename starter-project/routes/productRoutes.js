/**
 *  FILE INI PERLU DILENGKAPI
 *
 * Product Routes
 * Mendefinisikan endpoint untuk operasi CRUD produk
 *
 * Tugas:
 * Buat routing untuk 5 endpoint berikut:
 * - GET    /         -> getAllProducts
 * - GET    /:id      -> getProductById
 * - POST   /         -> createProduct
 * - PUT    /:id      -> updateProduct
 * - DELETE /:id      -> deleteProduct
 */

// TODO: Import Express Router
// Hint: const express = require('express');
//       const router = express.Router();

// TODO: Import product controller
// Hint: const productController = require('../controllers/productController');

// ==========================================
// ROUTES DEFINITION
// ==========================================

// TODO: GET / - Get all products
// Hint: router.get('/', productController.getAllProducts);

// TODO: GET /:id - Get product by ID
// Hint: router.get('/:id', productController.getProductById);

// TODO: POST / - Create new product
// Hint: router.post('/', productController.createProduct);

// TODO: PUT /:id - Update product
// Hint: router.put('/:id', productController.updateProduct);

// TODO: DELETE /:id - Delete product
// Hint: router.delete('/:id', productController.deleteProduct);

// TODO: Export router
// Hint: module.exports = router;
